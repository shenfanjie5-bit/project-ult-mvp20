{
  "additionalProperties": false,
  "description": "Portable reference to a canonical entity across downstream schemas.",
  "properties": {
    "canonical_id_rule_version": {
      "minLength": 1,
      "title": "Canonical Id Rule Version",
      "type": "string"
    },
    "display_name": {
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Display Name"
    },
    "entity_id": {
      "minLength": 1,
      "title": "Entity Id",
      "type": "string"
    },
    "entity_type": {
      "minLength": 1,
      "title": "Entity Type",
      "type": "string"
    }
  },
  "required": [
    "entity_id",
    "entity_type",
    "canonical_id_rule_version"
  ],
  "title": "EntityReference",
  "type": "object",
  "x-contract-version": "0.1.0",
  "x-source-model": "contracts.schemas.entities.EntityReference"
}
